import { Typography } from '@mui/material'
import { WhiteRoot } from '../../../styles/common'

const Header = () => {
  return (
    <WhiteRoot>
      <Typography variant='h2'>Slatki zalogaj</Typography>
    </WhiteRoot>
  )
}

export default Header
