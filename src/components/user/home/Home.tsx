import { Root } from '../../../styles/common'
import BigCarousel from '../../common/bigCarousel/BigCarousel'

const Home = () => {
  return (
    <Root>
      <BigCarousel />
    </Root>
  )
}

export default Home
